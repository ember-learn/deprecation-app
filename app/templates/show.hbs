<div id="sidebar" class="sidebar" role="complementary">
  <TableOfContents @sortedGroupedResults={{this.sortedGroupedResults}} />

  <div id="back-to-top"><a id="back-top-top" href="#">&#11014; Back to Top</a></div>
</div>

<div class="main has-sidebar" id="content" role="main">
  <h2>Deprecations Added in {{this.project}} {{this.version}}</h2>

  <p>
    What follows is a list of deprecations introduced to {{this.project}} during the {{this.version}}
  cycle.
  </p>
  <p>For more information on deprecations in {{this.project}}, see the <LinkTo @route="index">main deprecations page</LinkTo>.</p>

  {{#each sortedGroupedResults as |result|}}
    <h3 class="anchorable-toc">Deprecations Added in {{result.since}}</h3>

    {{this.renderId}}

    {{#each result.contents as |content|}}
      <DeprecationArticle @model={{content}} @renderIdOrUntil={{this.renderIdOrUntil}} />
    {{/each}}
  {{/each}}
</div>
